# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: PhotoAlbumJava
metadata:
  template: PhotoAlbumJava@0.0.1-beta

services:
  photoalbum:
    project: .
    language: java
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
