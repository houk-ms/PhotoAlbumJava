-- Health check script for PostgreSQL DB - test connection
-- Migrated from Oracle to PostgreSQL according to SQL check item 3: Remove FROM DUAL in SELECT statements that only return values.
SELECT 1;